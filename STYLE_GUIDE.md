# RinType Plugin — 文法规则总览

本文档记录 plugin 所有文法规则。添加新规则前请先查阅本文档，避免重复。

---

## 已实现的规则

### 1. 首尾空白 `trim-whitespace`
- 去除文本首尾的多余空白字符
- 严重度：warning

### 2. 连续空格 `consecutive-spaces`
- 将多个连续空格合并为一个
- 严重度：warning

### 3. 中英文间距 `cjk-spacing`
- 中文与英文、数字之间需要添加空格（中文标点除外）
- 示例：`你好world` → `你好 world`，`100个` → `100 个`
- 不触发：`c，` `100。` 等英文/数字与中文标点相邻的情况
- 严重度：error

### 4. 全角/半角 `fullwidth-halfwidth`
- 英文字母和数字应使用半角字符
- 示例：`Ａｂｃ` → `Abc`，`１２３` → `123`
- 严重度：error

### 5. 专有名词大小写 `capitalization`
- 专有名词应使用正确的大小写拼写
- 已收录：iOS、iPhone、macOS、GitHub、JavaScript、TypeScript、BitMart 等
- 当 Figma Text Case 为 TITLE 或 UPPER 时自动跳过
- 严重度：warning

### 6. 标点符号 `punctuation`
- 中文语境使用中文标点，英文语境使用英文标点
- 覆盖标点：，。！？；：（）↔ ,.!?;:()
- 严重度：error

### 7. 时间单位大小写 `time-units`
- 除"月"（M）大写外，其余时间单位均小写
- y（年）、M（月）、d（天）、h（小时）、m（分钟）、s（秒）
- 示例：`3Y` → `3y`，`5D` → `5d`，`2H` → `2h`，`30S` → `30s`
- 注意：`M` 存在歧义（月 vs 百万），当前跳过不处理
- 严重度：warning

### 8. 计量单位大小写 `quantity-units`
- 计量单位均大写：K（千）、M（百万）、B（十亿）
- K/B 无歧义直接校验；M 仅在前面有 `$` 等货币符号时判定为计量单位
- 示例：`10k` → `10K`，`$5m` → `$5M`，`2b` → `2B`
- 严重度：warning

### 9. 分割线首字母大写 `separator-capitalization`
- 分割线（`/` `|` `—` `–` `·`）前后的文本首字母应大写
- 前提：文本中必须含有分割线才触发检查
- 当 Figma Text Case 为 TITLE 或 UPPER 时自动跳过
- 示例：`design / development` → `Design / Development`
- 严重度：warning

### 10. 数字与货币/百分号间距 `number-spacing`
- 数字与货币单位名称之间需要空格：`100USD` → `100 USD`
- 数字与百分号之间不加空格：`100 %` → `100%`
- 货币符号与数字之间不加空格：`$ 50` → `$50`
- `≈` / `+` / `-` 应置于货币符号之前，且符号间无空格：`$+1.45` → `+$1.45`、`$ -1.45` → `-$1.45`、`$ ≈1.45` → `≈$1.45`
- 已收录货币单位：USD、EUR、GBP、JPY、CNY、USDT、USDC、BTC、ETH 等
- 严重度：error

### 11. 千分符 `thousand-separator`
- 四位及以上数字应添加千分符（年份和日期除外）
- 示例：`1000` → `1,000`，`1000000` → `1,000,000`
- 不处理：`2025年`、`2024/01/01`、已有千分符的数字、小数、ID 等
- 严重度：warning

### 12. 英文句首大写 `sentence-capitalization`
- 独立英文句子首字母应大写
- 句子边界：`.` `!` `?` `。` `！` `？` 后跟空格，或文本开头
- 已排除特殊词：iPhone、macOS、iOS 等 camelCase 专有名词
- 当 Figma Text Case 为 TITLE 或 UPPER 时自动跳过
- 示例：`this is good.` → `This is good.`
- 严重度：warning

### 13. 连字符与破折号 `dash-normalize`
- 区分连字符（`-`）、半角破折号（`–`）和全角破折号（`——`）
- 中文语境中 `--` 和 `–` 应使用 `——`
- 英文连字符（如 `well-known`）和范围符号（如 `2020–2024`）保持不变
- 严重度：warning

### 15. 运算符间距 `operator-spacing`
- 运算符（`+` `-` `×` `÷` `=` `≈`）作为运算符时两侧需要空格
- `+` `-` 作为正负符号时不受影响（左侧无数值）
- 示例：`6.26%+26.5%` → `6.26% + 26.5%`，`$1.45+$2.30` → `$1.45 + $2.30`
- 不触发：`+$1.45`、`-$1.45`（正负符号）
- 严重度：error

### 14. URL 前后空格 `url-spacing`
- URL 链接（`http://`、`https://`、`www.`）与中文之间需要添加空格
- 示例：`访问https://example.com` → `访问 https://example.com`
- 严重度：error

---

## 待实现的规则

### 15. 中文省略号
- 应使用 `……`（U+2026×2）而非 `...`（三个英文句号）

### 16. 重复标点
- 连续相同标点合并为一个：`！！！` → `！`

### 17. 标点前多余空格
- 标点符号前不应有空格：`你好 ，` → `你好，`

### 18. 中文引号
- 中文语境应使用直角引号 `「」`「『』」而非弯引号 `""` `''`

---

## 智能跳过

以下情况插件会自动跳过，不报告问题：

- **隐藏图层**：Figma 中不可见的图层及其子节点
- **Figma Text Case**：设置了 TITLE / UPPER 的文本节点，跳过大小写相关规则
- **年份和日期**：`2025年`、`2024/01/01` 等不添加千分符

---

## 备注

- 规则"英文与中文间距"和"数字与中文间距"已被 `cjk-spacing`（规则 3）覆盖，无需重复实现
- `M` 在时间单位（月）和计量单位（百万）之间存在歧义，当前通过 `$` 货币符号区分，后续待优化
